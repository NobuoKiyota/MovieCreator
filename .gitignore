node_modules/
dist/
*.zip
*.mp4
ai_archives/
.DS_Store
# data/scores.json (教師データ本体) はマルチPC間で同期する必要があるため追跡対象。
# 破損時フォールバック(apiHandler.js)が作るバックアップだけ除外する。
data/*.corrupted-*.json

# ProRes 4444変換用ffmpegバイナリ(PCごとに手動配置、CLAUDE.md参照)と変換時の一時ファイル
tools/ffmpeg/
.tmp_transcode/

# 書き出し動画の保存先(/api/save-export、VideoRecorder.js参照)。*.mp4は元々除外済みだが
# 透過WebM/ProRes MOVも同じフォルダに出力されるためディレクトリごと除外する。
output/

# Pythonキャッシュおよび自動化ツールの作業データ・一時ファイル
scripts/__pycache__/
X/
data/sns_pending_post.json
output/temp_protected_post.mp4
